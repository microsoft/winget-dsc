// Hello-world probe for the .NET flow.
//
// The simplest possible thing that proves the .NET 10 SDK installed and
// produced a working `dotnet` toolchain. Top-level statement; matches the
// `dotnet new console` template's idiomatic shape.

Console.WriteLine("Hello, world!");
